## Packages
framer-motion | Page transitions and list animations
date-fns | Human-readable date formatting

## Notes
Using installed shadcn/ui components.
API assumes /api/complaints endpoints as defined in routes manifest.
